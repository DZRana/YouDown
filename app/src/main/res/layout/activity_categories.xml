<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_categories"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.dzrana.youdown.Categories"
    android:background="@drawable/bg">

    <Button
        android:text="@string/food_cat"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="105dp"
        android:id="@+id/food_btn"
        android:background="@android:color/holo_red_dark"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:textColor="#FFF" />

    <Button
        android:text="@string/movie_cat"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/movie_btn"
        android:textColor="#FFF"
        android:layout_marginTop="46dp"
        android:layout_below="@+id/food_btn"
        android:layout_alignLeft="@+id/food_btn"
        android:layout_alignStart="@+id/food_btn"
        android:background="@android:color/holo_purple" />

    <Button
        android:text="@string/chill_cat"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/chill_btn"
        android:layout_marginTop="46dp"
        android:textColor="#FFF"
        android:layout_below="@+id/movie_btn"
        android:layout_alignLeft="@+id/movie_btn"
        android:layout_alignStart="@+id/movie_btn"
        android:background="@android:color/holo_blue_dark" />
</RelativeLayout>
